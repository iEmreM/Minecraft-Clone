moderngl>=5.8.2
pygame>=2.1.0
numpy>=1.21.0
PyGLM>=2.5.0
numba>=0.57.0
# psutil is optional: with it the F3 screen reports memory and CPU, without it
# those two lines are simply absent. Nothing else in the game reads it.
